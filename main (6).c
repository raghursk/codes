#include<stdio.h>
int main()
{
    char string[10];
    printf("entr the string:\n");
    scanf("%s",string);
    for(int i=0;string[i]!='\0';i++)
    {
        printf("%s",string);
    }
    char asc[256]={0};
    printf("fiend the leragest elements in array is :\n");
}